import { Field } from "formik";
import { styled } from "../../stitches.config";

export const StyledField = styled(Field, {
    marginRight: 15,
    height: 20,
    width: 200,
    marginBottom: 20
})